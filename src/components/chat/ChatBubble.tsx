"use client"

/**
 * STUB PUBLIC — version open-source.
 *
 * Bulle d'accès rapide à l'assistant IA. Le vrai composant ouvre le
 * `ChatPanel` privé et est désactivé dans la version publique.
 *
 * Pour ré-activer un assistant IA dans une instance auto-hébergée,
 * remplace ce fichier par ton implémentation.
 */

export function ChatBubble(): null {
  return null
}
