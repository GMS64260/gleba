"use client"

/**
 * STUB PUBLIC — version open-source.
 *
 * Le vrai widget de feedback (bouton flottant + formulaire de signalement de
 * bug / demande d'évolution, branché sur l'API /api/feedback) est un outil
 * opérationnel propre à l'instance gleba.fr et n'est pas distribué dans la
 * version auto-hébergée. Sur GitHub et pour tout clone « communauté », on
 * expose ce composant vide afin que le build Next.js passe.
 *
 * Pour ré-activer un système de feedback dans une instance auto-hébergée,
 * remplacez ce fichier par votre implémentation. Aucune prop n'est attendue.
 */

export function FeedbackWidget(): null {
  return null
}
